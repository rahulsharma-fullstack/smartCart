specVersion: "v1beta"
serviceId: "smartCart"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartCart-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
